import { TestBed } from '@angular/core/testing';

import { BasePersistenceService } from './base-persistence.service';

describe('BasePersistenceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));
});
